#!/bin/sh -e

echo "\n"
echo "You must allow docker to run as non-root user."
echo "To do this follow the steps in the official docker documentation: https://docs.docker.com/engine/install/linux-postinstall/#manage-docker-as-a-non-root-user"
echo "Run the following:"
echo "sudo usermod -aG docker \$USER"
echo "newgrp docker"